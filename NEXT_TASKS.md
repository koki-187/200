# 📋 次のチャットへのタスク引き継ぎ

## 📅 最終更新日時
2025-11-17 16:24 (JST)

---

## 🎯 現在のプロジェクト状態

### ✅ 完全に稼働中
- **URL**: https://3000-ihv36ugifcfle3x85cun1-5c13a017.sandbox.novita.ai
- **サービス状態**: PM2で稼働中（Online）
- **Git状態**: すべてコミット済み、GitHubと同期済み
- **バックアップ**: 作成済み（https://www.genspark.ai/api/files/s/Io14Zbjf）

### 📊 プロジェクト統計
- **コミット数**: 13件
- **総コード行数**: 3,933行（TypeScript: 2,618行 + JavaScript: 1,315行）
- **ファイル数**: 36ファイル
- **バージョン**: v1.1.0

---

## ✅ v1.2.0で実装完了した機能（最優先タスク完了）

### 🎉 新規実装機能（2025-11-17）

#### OCR自動入力機能 ✅
- [x] OpenAI Vision API統合（gpt-4o）
- [x] 画像・PDFから物件情報自動抽出
- [x] 新規案件作成モーダルにOCRボタン追加
- [x] 自動フォーム入力（物件名、所在地、駅、徒歩、面積、価格）
- [x] エラーハンドリングとユーザーフィードバック
- **実装ファイル**: `src/routes/ocr.ts`, `public/static/app.js`

#### メール通知システム ✅
- [x] Resend SDK統合
- [x] 期限通知メール（24時間前、緊急警告）
- [x] 新規メッセージ通知メール
- [x] 新規案件通知メール（エージェント向け）
- [x] テスト用APIエンドポイント（管理者専用）
- **実装ファイル**: `src/utils/email.ts`, `src/routes/email.ts`

#### PDFレポート生成 ✅
- [x] jsPDF統合（ブラウザ側生成）
- [x] 案件詳細PDFレポート（案件情報、担当者、AI提案、メッセージ履歴、ファイル一覧）
- [x] 案件詳細ページに「PDFレポート生成」ボタン追加
- [x] データ取得API（`/api/pdf/deal/:id/data`）
- [x] 複数案件サマリーAPI（`/api/pdf/deals/summary`）
- **実装ファイル**: `src/utils/pdf.ts`, `src/routes/pdf.ts`, `public/static/app.js`

### v1.1.0以前の機能（変更なし）

#### 認証・ユーザー管理
- [x] JWT認証システム（SHA-256ベース）
- [x] ロール管理（管理者/エージェント）
- [x] セッション管理・自動ログアウト
- [x] ユーザー作成・一覧表示（管理者専用）

#### ダッシュボード（案件一覧）
- [x] 案件カード表示（ステータス・期限バッジ付き）
- [x] リアルタイム検索（案件名、所在地、駅名）
- [x] 多様なソート（更新日時、作成日時、期限、案件名）
- [x] 高度なフィルター（ステータス、期限ステータス）
- [x] 新規案件作成モーダル（管理者専用）

#### 案件詳細ページ
- [x] 3カラムレイアウト（情報・ファイル | 未入力項目・チャット）
- [x] インライン編集（全フィールド編集可能）
- [x] ステータス変更ドロップダウン（即座反映）
- [x] 未入力項目パネル（リアルタイム警告）
- [x] ファイル管理
  - [x] 複数ファイルアップロード
  - [x] ストレージ使用状況表示（50MB上限）
  - [x] ファイル一覧・ダウンロード・削除
  - [x] ファイルタイプ別アイコン
- [x] チャット機能
  - [x] リアルタイムメッセージング
  - [x] 未読管理（買側・売側別）
  - [x] メッセージ履歴表示

#### お知らせページ
- [x] 通知一覧表示（タイムライン形式）
- [x] タイプ別フィルター（新規案件、メッセージ、期限、未入力項目）
- [x] 全て既読機能
- [x] 個別既読・削除機能

#### 設定ページ
- [x] ビジネスデイ設定（営業日選択）
- [x] 休日管理（祝日追加・削除）
- [x] ストレージ上限設定
- [x] ユーザー管理（管理者専用）
- [x] API情報表示

#### AI・その他
- [x] AI提案生成（OpenAI GPT-4）
- [x] 営業日48時間管理（自動期限計算）
- [x] ロールベースアクセス制御
- [x] Cloudflare D1データベース統合

---

## 🚀 次のチャットで優先的に取り組むべきタスク

### 🔴 優先度：最高（すぐに実装すべき）

#### ~~1. OCR自動入力機能の実装~~ ✅ 完了
**目的**: 画像・PDFから物件情報を自動抽出してフォームに入力
**状態**: **実装完了（2025-11-17）**

**実装ステップ**:
```typescript
// 1. OCRルート作成（src/routes/ocr.ts）
import { Hono } from 'hono';
import OpenAI from 'openai';

const ocr = new Hono();

ocr.post('/extract', async (c) => {
  // ファイルを受け取り
  const formData = await c.req.formData();
  const file = formData.get('file');
  
  // OpenAI Vision APIで解析
  const openai = new OpenAI({ apiKey: c.env.OPENAI_API_KEY });
  const response = await openai.chat.completions.create({
    model: "gpt-4o",
    messages: [{
      role: "user",
      content: [
        { type: "text", text: "この不動産資料から以下の情報を抽出してJSON形式で返してください: 所在地、最寄駅、徒歩分数、土地面積、用途地域、建蔽率、容積率、接道状況、現況、希望価格" },
        { type: "image_url", image_url: { url: imageBase64 } }
      ]
    }]
  });
  
  // 抽出結果を返す
  return c.json({ extracted: JSON.parse(response.choices[0].message.content) });
});
```

**フロントエンド統合**:
- 案件詳細ページに「OCR読取」ボタン追加
- ファイル選択後、自動的にフォームに入力
- 確認ダイアログで手動修正可能

**必要なファイル**:
- `src/routes/ocr.ts` (新規作成)
- `src/index.tsx` (ルート追加)
- `public/static/app.js` (OCR機能追加)

**期待される効果**:
- データ入力時間が大幅短縮（約80%削減）
- 入力ミスの減少
- ユーザー体験の向上

---

#### ~~2. メール通知システムの実装~~ ✅ 完了
**目的**: 期限通知、新規メッセージ通知を自動送信
**状態**: **実装完了（2025-11-17）**

**実装済み機能**:
- ✅ Resend SDK統合
- ✅ 期限通知メール（HTML形式、緊急度別スタイル）
- ✅ 新規メッセージ通知メール
- ✅ 新規案件通知メール（エージェント向け）
- ✅ テスト用APIエンドポイント（`/api/email/test/*`）

**旧実装ステップ（参考）**:
```typescript
// 1. メールユーティリティ作成（src/utils/email.ts）
import { Resend } from 'resend';

export class EmailService {
  private resend: Resend;
  
  constructor(apiKey: string) {
    this.resend = new Resend(apiKey);
  }
  
  async sendDeadlineNotification(to: string, dealTitle: string, deadline: string) {
    await this.resend.emails.send({
      from: 'notifications@your-domain.com',
      to,
      subject: `【期限通知】${dealTitle}の回答期限が迫っています`,
      html: `
        <h2>案件回答期限のお知らせ</h2>
        <p>案件「${dealTitle}」の回答期限が近づいています。</p>
        <p>期限: ${deadline}</p>
        <a href="https://your-app-url.com">案件を確認する</a>
      `
    });
  }
  
  async sendNewMessageNotification(to: string, dealTitle: string, sender: string) {
    await this.resend.emails.send({
      from: 'notifications@your-domain.com',
      to,
      subject: `【新着メッセージ】${dealTitle}に新しいメッセージがあります`,
      html: `
        <h2>新着メッセージ</h2>
        <p>案件「${dealTitle}」に${sender}さんから新しいメッセージが届きました。</p>
        <a href="https://your-app-url.com">メッセージを確認する</a>
      `
    });
  }
}

// 2. Cronジョブで定期実行（wrangler.jsonc）
{
  "triggers": {
    "crons": ["0 9 * * *"]  // 毎日9時に実行
  }
}
```

**必要なパッケージ**:
```bash
npm install resend
```

**環境変数追加（.dev.vars）**:
```
RESEND_API_KEY=your-resend-api-key
```

**統合ポイント**:
- 案件作成時に期限通知スケジュール設定
- メッセージ送信時に相手に通知
- 期限24時間前にリマインダー送信

---

#### ~~3. PDF一次回答レポート自動生成~~ ✅ 完了
**目的**: 案件情報とAI提案をPDF化して提出可能に
**状態**: **実装完了（2025-11-17）**

**実装済み機能**:
- ✅ jsPDF統合（ブラウザ側で生成）
- ✅ 案件詳細ページに「PDFレポート生成」ボタン
- ✅ 案件情報、担当者情報、AI提案、メッセージ履歴、ファイル一覧を含む完全なレポート
- ✅ 複数ページ対応、自動改ページ
- ✅ データ取得API（`/api/pdf/deal/:id/data`）
- ✅ 複数案件サマリーAPI（`/api/pdf/deals/summary`）

**旧実装ステップ（参考）**:
```typescript
// 1. PDFジェネレーター作成（src/utils/pdf.ts）
import { jsPDF } from 'jspdf';

export function generateDealReport(deal: Deal, proposal: Proposal): Buffer {
  const doc = new jsPDF();
  
  // ヘッダー
  doc.setFontSize(20);
  doc.text('物件一次回答レポート', 20, 20);
  
  // 案件基本情報
  doc.setFontSize(12);
  doc.text(`案件名: ${deal.title}`, 20, 40);
  doc.text(`所在地: ${deal.location}`, 20, 50);
  doc.text(`土地面積: ${deal.land_area}`, 20, 60);
  
  // AI提案内容
  doc.text('AI分析結果:', 20, 80);
  doc.setFontSize(10);
  doc.text(proposal.summary, 20, 90, { maxWidth: 170 });
  
  // PDF出力
  return doc.output('arraybuffer');
}

// 2. PDFダウンロードルート（src/routes/deals.ts）
deals.get('/:id/report', async (c) => {
  const dealId = c.req.param('id');
  const deal = await db.getDealById(dealId);
  const proposal = await db.getLatestProposal(dealId);
  
  const pdf = generateDealReport(deal, proposal);
  
  return new Response(pdf, {
    headers: {
      'Content-Type': 'application/pdf',
      'Content-Disposition': `attachment; filename="report_${dealId}.pdf"`
    }
  });
});
```

**フロントエンド**:
- 案件詳細ページに「PDFレポート出力」ボタン追加
- ワンクリックでダウンロード

---

### 🟡 優先度：高（次に実装すべき機能）

#### 4. メール通知の自動化（Cron Triggers）⏰
**現状**: テスト用APIエンドポイントのみ実装済み  
**目標**: 定期実行で自動通知を送信

**実装ステップ**:
```typescript
// 1. Cronトリガー設定（wrangler.jsonc）
{
  "triggers": {
    "crons": ["0 9,18 * * *"]  // 毎日9時と18時に実行
  }
}

// 2. Cronハンドラー作成（src/index.tsx）
export default {
  async fetch(request, env) {
    return app.fetch(request, env);
  },
  async scheduled(event, env, ctx) {
    // 期限24時間前の案件を検索
    const db = new Database(env.DB);
    const deals = await db.getDealsNearDeadline(24); // 24時間以内
    
    const emailService = createEmailService(env.RESEND_API_KEY);
    
    for (const deal of deals) {
      const seller = await db.getUserById(deal.seller_id);
      if (seller?.email) {
        await emailService.sendDeadlineNotification(
          seller.email,
          deal.title,
          deal.response_deadline,
          24
        );
      }
    }
  }
}
```

**必要な作業**:
- `wrangler.jsonc` にCron設定追加
- `src/index.tsx` に `scheduled` ハンドラー追加
- `src/db/queries.ts` に `getDealsNearDeadline()` メソッド追加

---

#### 5. ファイルストレージの完全実装（Cloudflare R2）📦
**現状**: ファイルメタデータのみDB保存  
**目標**: 実際のファイルをCloudflare R2に保存

**実装ステップ**:
```typescript
// 1. R2バケット作成
// npx wrangler r2 bucket create webapp-files

// 2. ファイルアップロード処理（src/routes/files.ts）
files.post('/deals/:dealId', async (c) => {
  const dealId = c.req.param('dealId');
  const formData = await c.req.formData();
  const file = formData.get('file') as File;
  
  // R2にアップロード
  const key = `${dealId}/${Date.now()}-${file.name}`;
  await c.env.R2.put(key, await file.arrayBuffer(), {
    httpMetadata: { contentType: file.type }
  });
  
  // DBにメタデータ保存
  await db.createFile({
    id: nanoid(),
    deal_id: dealId,
    filename: file.name,
    storage_path: key,
    size_bytes: file.size,
    file_type: file.type
  });
  
  return c.json({ success: true });
});

// 3. ファイルダウンロード
files.get('/:fileId', async (c) => {
  const fileId = c.req.param('fileId');
  const fileRecord = await db.getFileById(fileId);
  
  // R2から取得
  const object = await c.env.R2.get(fileRecord.storage_path);
  return new Response(object.body, {
    headers: { 'Content-Type': fileRecord.file_type }
  });
});
```

**wrangler.jsonc更新**:
```jsonc
{
  "r2_buckets": [
    {
      "binding": "R2",
      "bucket_name": "webapp-files"
    }
  ]
}
```

---

#### 6. パスワードハッシュのbcrypt化🔒
**現状**: SHA-256（簡易版）  
**目標**: bcryptで本番対応

```typescript
// src/utils/crypto.ts
import bcrypt from 'bcryptjs';

export async function hashPassword(password: string): Promise<string> {
  return await bcrypt.hash(password, 10);
}

export async function verifyPassword(password: string, hash: string): Promise<boolean> {
  return await bcrypt.compare(password, hash);
}
```

**注意**: seed.sqlのパスワードハッシュも更新必要

---

#### 6. JWT署名の強化
**現状**: 簡易Base64エンコード  
**目標**: HS256/RS256による署名

```typescript
// src/utils/crypto.ts
import jwt from '@tsndr/cloudflare-worker-jwt';

export async function generateToken(userId: string, role: string, secret: string): Promise<string> {
  return await jwt.sign({
    userId,
    role,
    exp: Math.floor(Date.now() / 1000) + (7 * 24 * 60 * 60)
  }, secret);
}

export async function verifyToken(token: string, secret: string): Promise<any> {
  return await jwt.verify(token, secret);
}
```

---

### 🟢 優先度：中（将来的に実装）

#### 7. ファイルプレビュー機能
- PDF/画像のインラインプレビュー
- Excelファイルのグリッド表示

#### 8. データエクスポート
- 案件一覧のCSV/Excelエクスポート
- 月次レポート自動生成

#### 9. 高度な検索・フィルタ
- 価格帯フィルター
- 地域別フィルター
- タグ機能

#### 10. LINE通知連携
- LINE Notify統合
- スマホでの即時通知

---

## 📁 重要なファイルとその役割

### コアファイル
| ファイル | 役割 | 編集頻度 |
|---------|------|---------|
| `src/index.tsx` | HTMLエントリーポイント、ルート定義 | 低 |
| `public/static/app.js` | フロントエンドJavaScript | 高 |
| `src/routes/` | APIエンドポイント | 中 |
| `src/db/queries.ts` | データベースクエリ | 中 |
| `wrangler.jsonc` | Cloudflare設定 | 低 |
| `package.json` | 依存関係、スクリプト | 低 |

### ドキュメント
| ファイル | 役割 |
|---------|------|
| `README.md` | プロジェクト全体説明 |
| `HANDOVER.md` | 引き継ぎドキュメント |
| `NEXT_TASKS.md` | このファイル（タスク管理） |
| `TEST_REPORT.md` | テストレポート |

---

## 🔧 開発ワークフロー

### 日常的な開発サイクル
```bash
# 1. コード編集
vim src/routes/new-feature.ts

# 2. ビルド
npm run build

# 3. サービス再起動
pm2 restart webapp

# 4. 動作確認
curl http://localhost:3000/api/new-endpoint

# 5. コミット
git add .
git commit -m "feat: 新機能実装"
git push origin main
```

### データベース変更時
```bash
# 1. マイグレーションファイル作成
cat > migrations/0002_add_new_field.sql << 'EOF'
ALTER TABLE deals ADD COLUMN new_field TEXT;
EOF

# 2. マイグレーション適用
npm run db:migrate:local

# 3. seed.sql更新（必要に応じて）
vim seed.sql

# 4. データベースリセット
npm run db:reset
```

### 新しいAPIルート追加時
```bash
# 1. ルートファイル作成
vim src/routes/new-route.ts

# 2. src/index.tsx にルート追加
import newRoute from './routes/new-route';
app.route('/api/new-route', newRoute);

# 3. フロントエンドから呼び出し
// public/static/app.js
axios.get('/new-route/endpoint');
```

---

## 🐛 既知の問題と回避策

### 1. ファイルアップロードの制限
**問題**: 実際のファイルがR2に保存されていない  
**回避策**: メタデータのみDB保存（タスク4で解決予定）

### 2. OpenAI APIキーが未設定の場合
**問題**: AI提案生成が失敗  
**回避策**: `.dev.vars`に`OPENAI_API_KEY`を設定

### 3. PM2再起動時のメモリリーク
**問題**: 長時間稼働後にメモリ使用量増加  
**回避策**: 定期的に`pm2 restart webapp`

### 4. D1データベースの同期問題
**問題**: ローカルと本番のデータ不一致  
**回避策**: マイグレーションを必ず両方に適用

---

## 📞 緊急時の対応

### サービスが停止した場合
```bash
# 1. ポートクリーンアップ
fuser -k 3000/tcp 2>/dev/null || true

# 2. PM2完全停止
pm2 delete all

# 3. 再起動
npm run build
pm2 start ecosystem.config.cjs

# 4. ログ確認
pm2 logs webapp --nostream
```

### データベースが壊れた場合
```bash
# データベース完全リセット
npm run db:reset
```

### GitHubとの同期が取れない場合
```bash
# 強制プル
git fetch origin
git reset --hard origin/main

# または強制プッシュ
git push -f origin main
```

---

## 🎯 成功の指標（KPI）

### 実装完了の定義
- [ ] OCR機能が動作し、自動入力成功率80%以上
- [ ] メール通知が正常に送信される
- [ ] PDFレポートが正しくフォーマットされている
- [ ] ファイルアップロード・ダウンロードが正常動作
- [ ] 全テストが合格（100%成功率）

### ユーザー体験の目標
- [ ] データ入力時間が従来の1/5以下
- [ ] 案件作成から一次回答まで48時間以内達成率95%以上
- [ ] ファイルアップロードの失敗率5%以下
- [ ] システム応答時間2秒以内（90パーセンタイル）

---

## 📚 参考リソース

### ドキュメント
- Cloudflare Workers: https://developers.cloudflare.com/workers/
- Hono Framework: https://hono.dev/
- Cloudflare D1: https://developers.cloudflare.com/d1/
- OpenAI API: https://platform.openai.com/docs/
- Resend API: https://resend.com/docs/

### コミュニティ
- Hono Discord: https://discord.gg/hono
- Cloudflare Community: https://community.cloudflare.com/

---

## ✅ 引き継ぎチェックリスト

次のチャット開始時に確認すべき項目:

- [x] プロジェクトが存在する（/home/user/webapp）
- [x] GitHubと同期されている
- [x] サービスが起動している（PM2）
- [x] 公開URLにアクセス可能
- [x] ログイン動作確認
- [x] 全機能が正常動作
- [x] 引き継ぎドキュメントを読んだ
- [ ] 次のタスクを選択した
- [ ] 実装計画を立てた

---

## 🚀 次のチャットでの最初の一歩

### ステップ1: 環境確認
```bash
cd /home/user/webapp
git status
pm2 list
curl http://localhost:3000/api/health
```

### ステップ2: タスク選択
上記の「次のチャットで優先的に取り組むべきタスク」から1つ選択

### ステップ3: 実装開始
選択したタスクの「実装ステップ」に従って開発開始

---

**📌 推奨される最初のタスク**: **OCR自動入力機能の実装**

理由:
- ユーザー体験への影響が最も大きい
- データ入力時間を80%削減できる
- OpenAI Vision APIの統合経験が得られる
- 他のタスクの基礎となる

---

**🎯 次のチャットで会いましょう！幸運を祈ります！**

---

## 📝 最終更新履歴

| 日時 | 更新者 | 内容 |
|------|--------|------|
| 2025-11-17 16:24 | AI Assistant | 初版作成 |
| 2025-11-17 16:24 | AI Assistant | 優先タスクの詳細追加 |
| 2025-11-17 16:24 | AI Assistant | 実装ステップの具体化 |

---

**🔗 関連ドキュメント**:
- [README.md](./README.md) - プロジェクト全体説明
- [HANDOVER.md](./HANDOVER.md) - 引き継ぎドキュメント
- [TEST_REPORT.md](./TEST_REPORT.md) - テストレポート

**🌐 リンク**:
- **GitHub**: https://github.com/koki-187/200
- **公開URL**: https://3000-ihv36ugifcfle3x85cun1-5c13a017.sandbox.novita.ai
- **バックアップ**: https://www.genspark.ai/api/files/s/Io14Zbjf
